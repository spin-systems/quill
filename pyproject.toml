[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm[toml]>=3.4"]

[tool.setuptools_scm]
local_scheme = lambda version: ""
version_scheme = lambda version: ".".join([version.tag.base_version, str(version.distance)])
